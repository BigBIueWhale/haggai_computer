# Fish is the account's login shell. Keep the system-wide developer tool paths
# available in interactive sessions without printing anything at shell startup.
fish_add_path --global --move /usr/local/bin /home/user/.local/bin
